import React from "react";
import RouterComponent from "./router";

const App = () => {
  return (
    <div className="App">
      <RouterComponent />
    </div>
  );
};

// export default withRouter(App);
export default App;
